#include <iostream>
using namespace std;
void Occurence(int arr[], int st, int en, int ele)
{
    int firstOccurence = -1;
    if (st > en)
    {
        return
    }
}
int main()
{
    int arr[10] = {1, 2, 5, 6, 8};
    int s = 0, e = 5, element;
    bool ans;
    cout << "Enter the element to be found";
    cin >> element;
    Occurence(arr, s, e, element);
    return 0;
}